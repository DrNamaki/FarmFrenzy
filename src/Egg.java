public class Egg extends Product{

    static int Price = 15 ;
    static int SizeInWareHouse = 1 ;
    static int Time = 2 ;
    public Egg(int currentTime, int x_position, int y_position, boolean harvested) {
        super(currentTime, x_position, y_position, harvested);
    }
}
